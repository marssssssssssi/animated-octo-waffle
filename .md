# Блок-схема программы мониторинга пиковой нагрузки портала

## Описание программы
Программа суммирует N интервалов времени (10 ≤ N ≤ 30) и вычисляет общее время работы портала.

## Диаграмма алгоритма

```mermaid
flowchart TD
    Start([Начало]) --> A[Вывод заголовка]
    A --> B[Ввод N<br>10 ≤ N ≤ 30]
    
    B --> C{N в диапазоне<br>10-30?}
    C -->|Нет| D[Вывод ошибки]
    D --> End1([Конец программы])
    
    C -->|Да| E[Выделение памяти<br>для массива intervals]
    
    E --> F{Память выделена?}
    F -->|Нет| G[Вывод ошибки памяти]
    G --> End2([Конец программы])
    
    F -->|Да| H[Вызов inputIntervals]
    
    subgraph H[Функция inputIntervals]
        H1[Вывод приглашения] --> H2[Цикл i=0..N-1]
        H2 --> H3[Вывод номера интервала]
        H3 --> H4[Ввод hours<br>0 ≤ hours ≤ 23]
        H4 --> H5[Ввод minutes<br>0 ≤ minutes ≤ 59]
        H5 --> H6[Ввод seconds<br>0 ≤ seconds ≤ 59]
        H6 --> H7[Увеличение i]
        H7 --> H8{i < N?}
        H8 -->|Да| H3
        H8 -->|Нет| H9[Выход из функции]
    end
    
    H --> I[Вывод заголовка<br>Зафиксированные интервалы]
    I --> J[Цикл вывода всех интервалов]
    J --> K[Вызов printTime для каждого]
    K --> L[Вызов sumIntervals]
    
    subgraph L[Функция sumIntervals]
        L1[totalSeconds = 0] --> L2[Цикл i=0..N-1]
        L2 --> L3[Вызов toSeconds<br>для intervals[i]]
        L3 --> L4[totalSeconds += результат]
        L4 --> L5[Увеличение i]
        L5 --> L6{i < N?}
        L6 -->|Да| L3
        L6 -->|Нет| L7[Вызов normalizeTime<br>с totalSeconds]
        
        subgraph L7[Функция normalizeTime]
            M1[Вычисление hours<br>= totalSeconds / 3600]
            M2[Вычисление minutes<br>= остаток / 60]
            M3[Вычисление seconds<br>= остаток % 60]
            M4[Возврат структуры TimeInterval]
        end
        
        L7 --> L8[Возврат результата]
    end
    
    L --> N[Вывод разделителя]
    N --> O[Вызов printTime для total]
    O --> P[Вывод времени текстом]
    P --> Q[Освобождение памяти<br>delete[] intervals]
    Q --> End3([Конец программы])
    
    subgraph R[Функция toSeconds]
        R1[Возврат<br>hours*3600 + minutes*60 + seconds]
    end
    
    subgraph S[Функция printTime]
        S1[Установка fill='0'] --> S2[Вывод с setw2<br>HH:MM:SS]
        S2 --> S3[Выход из функции]
    end
```

## Условные обозначения
- ⭕ - Начало/конец программы
- ▭ - Процесс (действие)
- ◇ - Решение (условие)
- ⎾ ⏋ - Подпрограмма (функция)

## Ссылки
- [Исходный код программы](time_monitoring.cpp)
- [Mermaid Live Editor](https://mermaid.live) - для редактирования диаграммы
